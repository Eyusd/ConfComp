enclave {
    from "openenclave/edl/syscall.edl" import *;
    from "platform.edl" import *;

    trusted {
        public void enclave_parser(
            char* str,
            [out] double* output);
    };

    untrusted {
    };
};